# 20.04 is a LTS (long-term support) version

FROM julianqz/ubuntu:main

LABEL maintainer="Julian Zhou [jqzhou@email.wustl.edu]" \
      description="Minimal ubuntu with /bin/sh for LSF"

# LSF system needs this
ENTRYPOINT ["/bin/sh"]
